name: fix-code-style

on:
  push:

jobs:
  fix-code-style:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT }}
          ref: ${{ github.head_ref }}
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'
      - name: Install dependencies
        run: |
          sudo apt-get install php8.1-mongodb make php-pear
          sudo pecl install mongodb
          sudo echo "extension=mongo.so" | sudo tee /etc/php8.1/mods-available/mongo.ini
          composer install --no-interaction
          bun install
      - name: Run Pint
        run: ./vendor/bin/pint
      - name: Pull changes
        run: git pull
      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: 'chore: fix code style'
